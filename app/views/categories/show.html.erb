<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> | <%=
    "$#{item.price.to_f/100}" %> </li>
  <%= form_tag "/line_items/#{item.id}" do %>
    <%= submit_tag "Add to Cart" %>
  <% end %>
<% end %>
</ul>
